import { SubtractStringPipe } from './subtract-string.pipe';

describe('SubtractStringPipe', () => {
  it('create an instance', () => {
    const pipe = new SubtractStringPipe();
    expect(pipe).toBeTruthy();
  });
});
